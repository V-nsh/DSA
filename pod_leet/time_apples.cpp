#include<iostream>
#include<vector>

using namespace std;

class Solution {
public:
    int minTime(int n, vector<vector<int>>& edges, vector<bool>& hasApple) {
        int time = 0;
        vector<vector<bool>> visited(edges.size(), vector<bool> (edges.size(), false));

        for (int i = 0; i < edges.size(); i++)
        {
            for (int j = 0; j < edges.size(); j++)
            {
                
            }
            
        }
        
    }
};